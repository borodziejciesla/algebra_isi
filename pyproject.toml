[project]
name = "algebra-isi"
version = "0.1.0"
description = "Materiały i przykłady do algebry"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
]

[dependency-groups]
notebooks = [
    "ipykernel>=6.29",
    "jupyterlab>=4.2",
]

[tool.uv]
package = false
